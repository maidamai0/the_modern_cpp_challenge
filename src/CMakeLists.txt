file(GLOB srcs *.cpp)
foreach(src ${srcs})
    get_filename_component(problem ${src} NAME_WLE)
    add_executable(${problem} ${src})
    target_compile_definitions(${problem} PRIVATE FILE_NAME="${problem}")
    add_test(${problem}_test ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/${problem})
endforeach()